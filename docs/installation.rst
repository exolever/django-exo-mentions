============
Installation
============

At the command line::

    $ easy_install django-mentions

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mentions
    $ pip install django-mentions
